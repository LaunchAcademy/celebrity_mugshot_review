require 'spec_helper'

describe User do

  it "requires a valid first name" do
  end

  it "requires a valid last name"
  it "requires a valid email"
  it "requires a valid user name"
  it "requires a secure password"
  it "creates a new user when passed valid paramters"
  it "has a method to return a full name"

end
